model FaqBot {
    id           String   @id @default(uuid())
    user_message String
    bot_response String?
    createdAt    DateTime @default(now())
    updatedAt    DateTime @default(now()) @updatedAt
}
