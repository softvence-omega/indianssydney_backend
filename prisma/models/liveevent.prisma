model LiveEvent {
  id         String   @id @default(uuid())
  title      String
  subTitle   String
  chooseDate String
  chooseTime String
  thumbail   String
  tags       String[]
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}
