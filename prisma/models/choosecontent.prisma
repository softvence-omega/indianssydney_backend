model ChooseCatrogy {
  id          String      @id @default(uuid())
  contenttype ContentType
  description String
  caticon     String

  @@map("chooose-category")
}

enum ContentType {
  ARTICLE
  PODCAST
  VIDEO
  LIVE_EVENT
}
