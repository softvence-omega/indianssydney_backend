model Recommendation {
  id          String @id @default(uuid())
  name        String
  title       String
  description String
  icon        String
  userId      String
  user        User   @relation(fields: [userId], references: [id])

  @@map("recommendation")
}
